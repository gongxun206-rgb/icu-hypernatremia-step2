# Amsterdam extraction

AmsterdamUMCdb extraction is implemented in `src/models/run_amsterdam_harmonized.py` because source times are stored as database-relative integer offsets. Connection values are supplied through environment variables; no credentials are stored here.
