# Environment notes

Exact historical Python, principal-package, and MIMIC PostgreSQL versions were not preserved in the frozen analysis artifacts. The requirements file is therefore a compatibility template, not a claim about the historical environment. The local Amsterdam database audit recorded PostgreSQL 17, but that does not establish the earlier MIMIC analysis environment.
